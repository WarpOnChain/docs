import { ThemeProvider } from "next-themes";
import Script from "next/script";
import { inter, plus_jakarta_sans } from "../fonts";
import ClientOnlyAskCookbook from "../components/ClientOnlyAskCookbook";
import "../globals.css";

export default function App({ Component, pageProps }) {
  return (
    <ThemeProvider attribute="class">
      <Script
        id="schema-markup"
        type="application/ld+json"
        strategy="beforeInteractive"
        dangerouslySetInnerHTML={{
          __html: JSON.stringify({
            "@context": "https://schema.org",
            "@type": "WebSite",
            name: "Warp Documentation",
            description:
              "Comprehensive documentation for Warp, a Layer 3 (L3) blockchain built on Arbitrum'sOrbit Chain",
            url: "https://docs.warponchain.com",
            publisher: {
              "@type": "Organization",
              name: "Warp",
              url: "https://warponchain.com",
              sameAs: [
                "https://x.com/WarpOnChain",
                "https://github.com/WarpOnChain",
                "https://t.me/WarpPortal",
              ],
            },
          }),
        }}
      />
      <div
        className={`${inter.variable} ${plus_jakarta_sans.variable} font-sans`}
      >
        <div className="bg-white dark:bg-magic-black">
          <Component {...pageProps} />
        </div>
      </div>
      <ClientOnlyAskCookbook />
    </ThemeProvider>
  );
}
